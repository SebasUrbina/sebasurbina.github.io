export type BlogMetadata = {
  title: string;
  date: string;
  excerpt: string;
  slug: string;
  tags: string[];
};

export type BlogPost = {
  title: string;
  date: string;
  excerpt: string;
  content: string;
  slug: string;
  tags: string[];
};
